# library-management-
A simple Library Management System developed using Python and SQLite, featuring user authentication, role-based access, CRUD operations, and database management through a command-line interface.
